rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    function signedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return signedIn() && request.auth.uid == userId;
    }

    function isConnected(userId) {
      return signedIn() && (
        exists(/databases/$(database)/documents/users/$(request.auth.uid)/connections/$(userId)) &&
        exists(/databases/$(database)/documents/users/$(userId)/connections/$(request.auth.uid))
      );
    }

    function validConnectionDocFor(userId, connectionId) {
      return signedIn()
        && connectionId == request.auth.uid
        && request.resource.data.uid == request.auth.uid
        && request.resource.data.keys().hasOnly(['uid', 'fullName', 'connectionCode', 'authorizedByCode', 'connectedAt'])
        && request.resource.data.fullName is string
        && request.resource.data.connectionCode is string
        && request.resource.data.authorizedByCode is string
        && exists(/databases/$(database)/documents/users/$(request.auth.uid))
        && exists(/databases/$(database)/documents/connectCodes/$(request.resource.data.authorizedByCode))
        && get(/databases/$(database)/documents/connectCodes/$(request.resource.data.authorizedByCode)).data.uid == userId;
    }

    match /users/{userId} {
      allow read, write: if isOwner(userId);

      match /connections/{connectionId} {
        allow read, write: if isOwner(userId);
        allow create, update: if validConnectionDocFor(userId, connectionId);
        allow delete: if signedIn() && connectionId == request.auth.uid;
      }

      match /pushSubscriptions/{subscriptionId} {
        allow read: if isOwner(userId) || isConnected(userId);
        allow write: if isOwner(userId);
      }

      match /alerts/{alertId} {
        allow read, update, delete: if isOwner(userId);
        allow create: if signedIn()
          && request.resource.data.senderUid == request.auth.uid
          && isConnected(userId);
      }
    }

    match /connectCodes/{code} {
      allow get: if signedIn();
      allow list: if false;
      allow create: if signedIn()
        && request.resource.data.uid == request.auth.uid
        && request.resource.data.connectionCode == code
        && request.resource.data.keys().hasOnly(['uid', 'fullName', 'connectionCode', 'updatedAt']);
      allow update: if signedIn()
        && resource.data.uid == request.auth.uid
        && request.resource.data.uid == request.auth.uid
        && request.resource.data.connectionCode == code
        && request.resource.data.keys().hasOnly(['uid', 'fullName', 'connectionCode', 'updatedAt']);
      allow delete: if signedIn() && resource != null && resource.data.uid == request.auth.uid;
    }

    match /publicProfiles/{userId} {
      allow read: if false;
      allow create, update, delete: if isOwner(userId);
    }
  }
}
